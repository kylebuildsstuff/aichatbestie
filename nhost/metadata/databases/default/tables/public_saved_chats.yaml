table:
  name: saved_chats
  schema: public
insert_permissions:
  - role: user
    permission:
      check:
        user_id:
          _eq: X-Hasura-User-Id
      columns:
        - id
        - user_id
        - chats
        - created_at
        - updated_at
select_permissions:
  - role: hasura
    permission:
      columns:
        - chats
        - created_at
        - updated_at
        - id
        - user_id
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns:
        - id
        - user_id
        - chats
        - created_at
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
update_permissions:
  - role: user
    permission:
      columns:
        - id
        - user_id
        - chats
        - created_at
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      check:
        user_id:
          _eq: X-Hasura-User-Id
delete_permissions:
  - role: user
    permission:
      filter:
        user_id:
          _eq: X-Hasura-User-Id
